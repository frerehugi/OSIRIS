{
  "name": "apis-keeper",
  "version": "0.1.0",
  "description": "Apis keeper — Cloudflare Worker (Cron Trigger) that reads open ConditionalSellOrder orders and executes them once their price condition is met.",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "viem": "2.55.19"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "wrangler": "4.114.0"
  }
}
